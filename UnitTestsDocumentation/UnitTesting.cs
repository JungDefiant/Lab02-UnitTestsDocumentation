using System;
using Xunit;

namespace UnitTestsDocumentation
{
    public class UnitTesting
    {
        // TEST 1 (Withdraw): Check if balance returns correctly when withdrawing certain amounts

        // TEST 2 (Withdraw): Check that user cannot withdraw more than balance

        // TEST 4 (Withdraw): Check that user cannot withdraw below 0

        // TEST 5 (Deposit): Check if balance returns correctly when depositing certain amounts

        // TEST 6 (Deposit): Check if deposit fails if depositing a negative amount

        // TEST 7 (ViewBalance): Check if program returns correct balance

        // TEST 8 (ViewBalance): Check if program returns correct balance after several transactions
    }
}
